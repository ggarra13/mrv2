#!/usr/bin/bash

USE_MSVC=1
if [[ $USE_MSVC == 1 ]]; then
    # if [[ -e /usr/mingw64/g++.exe ]]; then
    # 	pacman -R --noconfirm mingw-w64-x86_64-g++
    # fi
    # if [[ -e /usr/mingw64/gcc.exe ]]; then
    # 	pacman -R --noconfirm mingw-w64-x86_64-gcc
    # fi
fi
